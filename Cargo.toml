[package]
name = "event_logger"
version = "0.1.0"
edition = "2021"

[dependencies]
scylla = "0.7"
tokio = { version = "1", features = ["full"] }
rand = "0.8"
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4"] }  # Add this line